# Square duct

Planned training case: `Re_b_2600/`. Test cases: `Re_b_1100/` and `Re_b_1800/`.

OpenFOAM v1912 with `RASModel kOmega`. Each case will contain initial/boundary fields (`0/`), physical and turbulence settings (`constant/`), and mesh/numerical controls (`system/`).

Reference DNS: Pinelli et al. (2010); data are not included. Currently documentation only.
