# compressibleAdjointFoam

Compressible adjoint counterpart for jet in crossflow. Jet-in-crossflow case files and LES fields are excluded.

Former name: jicAdjointFoam. Target: OpenFOAM v1912.

Planned files: `compressibleAdjointFoam.C`, `createFields.H`, `Make/files`, and `Make/options`.

Documentation-only placeholder: no solver source or closure implementation is included yet. Equation references follow the current manuscript.
