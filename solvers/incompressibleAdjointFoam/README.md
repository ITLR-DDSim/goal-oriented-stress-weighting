# incompressibleAdjointFoam

Adjoint of the incompressible RANS equations for square duct and periodic hill (Eq. B6).

Former name: pehillAdjointFoam. Target: OpenFOAM v1912.

Planned files: `incompressibleAdjointFoam.C`, `createFields.H`, `Make/files`, and `Make/options`.

Documentation-only placeholder: no solver source or closure implementation is included yet. Equation references follow the current manuscript.
