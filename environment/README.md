# Environment and metadata

Intended dependencies: OpenFOAM v1912 and Python 3 with NumPy.

The implementation will include verified build order and run instructions. General workflow: load the OpenFOAM environment; compile the required custom solvers; prepare the case and external inputs; run the relevant baseline, frozen-omega and adjoint calculations; construct sensitivities and weights. Precise commands will be checked when source code is added.

Metadata still to add: a maintainer-approved root `LICENSE` and `CITATION.cff` with confirmed citation details. No licence is selected by this documentation scaffold.
