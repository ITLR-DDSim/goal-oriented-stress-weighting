# periodic-hill: alpha_1.5

Training condition. Planned OpenFOAM v1912 baseline with `RASModel kOmega`.

Planned contents:

- `0/`: initial fields and boundary conditions.
- `constant/`: physical properties, turbulence settings and required mesh material.
- `system/`: mesh-generation, numerical and run controls.

Documentation-only placeholder. No case dictionaries, simulation outputs, reference data or learned closure are supplied yet. Run instructions will accompany the case files.
